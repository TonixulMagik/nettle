# Nettle — legal documents

Terms of Service and Privacy Policy for **Nettle**, a Discord bot for support tickets,
self-assignable roles, giveaways and polls.

These pages are served through GitHub Pages so they stay reachable at a stable public URL.

| Document | Link |
|---|---|
| Terms of Service | https://tonixulmagik.github.io/Roost/terms.html |
| Privacy Policy | https://tonixulmagik.github.io/Roost/privacy.html |
| Support server | https://discord.gg/nettle |

> **Why the address still says `Roost`.** That is the repository name, and GitHub
> Pages builds the URL from it. The bot was renamed to Nettle; the repository was
> not. Both links above are live and serve the Nettle documents.
>
> The `/nettle/` addresses written here before returned **404** and were never
> reachable. Those are the ones that must never be given to Discord: a Terms of
> Service link that 404s fails verification on the spot.
>
> If the repository is ever renamed to `nettle`, these two links change with it,
> and the ones registered in the Developer Portal have to be updated in the same
> hour. Doing that *after* verification is the risky moment, not before.

## What Nettle reads

On a server it is added to, Nettle stores message text from **ticket channels only**, and only
to build the transcript that the server's own staff receive when a ticket is closed. It does not
log your server's normal channels and it does not read direct messages. Where activity levels
are switched on, it checks how long a message is and then discards the text without saving it.

One feature runs on the developer's own server only: mentioning the bot sends that message to
Google's Gemini API to generate a reply. It never runs anywhere else. The full detail, including
that feature, is in the Privacy Policy, section 4.

## Contact

For data deletion requests, questions, or anything else: **tonixbusiness.ro@gmail.com**

## Note

Nettle is in active development. Features are added, changed and removed, and both documents
are updated whenever that changes what the bot stores. The date at the top of each page always
says when it was last true.

Nettle is not affiliated with Discord Inc.
